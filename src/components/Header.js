import React from "react";

const Header = (props) => {
  return (
    <a href="/">
      <div className={"header"}>
        <div className={"logo"} />
      </div>
    </a>
  );
};

export default Header;
